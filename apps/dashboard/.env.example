# Gateway connection
NEXT_PUBLIC_GATEWAY_URL=http://localhost:18789
NEXT_PUBLIC_GATEWAY_TOKEN=your-gateway-token

# Optional: Override at runtime
# GATEWAY_URL=https://gateway.example.com

# Dashboard login password (server-side only — never prefix with NEXT_PUBLIC_)
DASHBOARD_SECRET=change-me-to-a-strong-secret
